{
  "name": "agri-guard",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered crop disease detection and smart agriculture platform",
  "scripts": {
    "dev": "npm --prefix frontend-repo run dev",
    "dev:frontend": "npm --prefix frontend-repo run dev",
    "dev:backend": "cd backend && venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo \"Backend deployment - no frontend build\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuvan-p-m/agri-guard-backend.git"
  },
  "license": "MIT"
}
